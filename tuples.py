# it's a container to store data, immutable which is not changable, but list can be edible
coordinates = (4, 5)
# coordinates[1] = 9 will show an error
print(coordinates[0])

lists_of_coordinates = [(1, 2), (3, 4), (5, 4)]

